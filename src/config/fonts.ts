import { Inter, Montserrat_Alternates } from "next/font/google";


export const inter = Inter({ subsets: ["latin"] });

// Nueva fuente | Fuente que se quiere utilizar
export const titleFont = Inter({ 
    subsets: ["latin"],
    weight: ['500', '700'], 
});